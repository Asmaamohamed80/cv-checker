# Patches

This folder contains patches for npm packages used in the project.

## wouter@3.7.1.patch

This patch adds route collection functionality to the wouter library, allowing the application to track all registered routes at runtime.

**Important:** This folder MUST be included when deploying to ensure proper package installation.
