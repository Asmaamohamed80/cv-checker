[phases.setup]
nixPkgs = ["nodejs_latest", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "NODE_ENV=production pnpm exec tsx server/_core/index.ts"
